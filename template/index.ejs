<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Phượng nguuuu!</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        width: 100vw;
        overflow-x: hidden;
      }

      ul {
        padding-left: 20px;
        padding-right: 20px;
        width: 100vw;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 15px;
        list-style-type: none;
        padding-bottom: 150px;
      }

      li {
        display: flex;
        align-items: center;
      }

      .image_item {
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
        border-radius: 5px;
        max-width: 350px;
        max-height: 450px;
      }

      .title {
        margin-top: 32px;
        margin-bottom: 45px;
        text-align: center;
        color: red;
      }
    </style>
  </head>
  <body>
    <h1 class="title">Welcome to Phượng Ngáo Site!</h1>

    <ul>
      <% photos.forEach(img => { %>
      <li>
        <img src="<%= img %>" alt="image" class="image_item" />
      </li>
      <% }) %>
    </ul>
  </body>
</html>
